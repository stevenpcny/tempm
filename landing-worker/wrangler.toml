name = "landing-worker"
main = "src/index.ts"
compatibility_date = "2024-12-01"
account_id = "YOUR_CLOUDFLARE_ACCOUNT_ID"

# Add your domains here (one entry per domain, with and without www)
# The AI_SETUP_GUIDE.md landing-worker section will fill these in automatically
routes = [
  { pattern = "yourdomain.top/*", zone_name = "yourdomain.top" },
  { pattern = "www.yourdomain.top/*", zone_name = "yourdomain.top" },
]
